# Reproduzierbare Zeilenenden (ANWEISUNG.md §11).
* text=auto eol=lf

# Windows-Skripte behalten CRLF im Working Tree.
*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf

# Binärdateien (falls je gezielt whitelisted) nicht anfassen.
*.png binary
*.exe binary
*.dll binary
*.pdb binary
